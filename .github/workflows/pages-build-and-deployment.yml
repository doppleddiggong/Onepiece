name: Deploy to GitHub Pages

on:
  # `honkit.yml`의 `build` 잡이 성공적으로 완료되면 자동으로 실행
  workflow_run:
    workflows: [ "Generate HonKit" ]
    types:
      - completed

  # 수동으로 배포할 때 사용
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  notify:
    runs-on: ubuntu-latest
    if: success()
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download GitHub Pages artifact
        id: download_artifact
        uses: dawidd6/action-download-artifact@v6
        continue-on-error: true
        with:
          workflow: honkit.yml
          workflow_conclusion: success
          name: github-pages
          path: ./downloaded

      - name: Extract deployment metadata
        id: metadata
        run: |
          set -euo pipefail

          EVENT_NAME=""
          TRIGGER=""
          LATEST_FILE=""

          if [ -d downloaded ]; then
            METADATA_FILE=""
            if [ -f downloaded/github-pages/metadata.json ]; then
              METADATA_FILE="downloaded/github-pages/metadata.json"
            elif [ -f downloaded/metadata.json ]; then
              METADATA_FILE="downloaded/metadata.json"
            fi

            if [ -n "$METADATA_FILE" ]; then
              EVENT_NAME=$(jq -r '.event_name // ""' "$METADATA_FILE")
              TRIGGER=$(jq -r '.trigger // ""' "$METADATA_FILE")
              LATEST_FILE=$(jq -r '.latest_file // ""' "$METADATA_FILE")
            fi
          fi

          echo "event_name=$EVENT_NAME" >> $GITHUB_OUTPUT
          echo "trigger=$TRIGGER" >> $GITHUB_OUTPUT
          echo "latest_file=$LATEST_FILE" >> $GITHUB_OUTPUT

      - name: Send Discord Notification
        env:
          EVENT_NAME: ${{ steps.metadata.outputs.event_name }}
          TRIGGER: ${{ steps.metadata.outputs.trigger }}
          LATEST_FILE: ${{ steps.metadata.outputs.latest_file }}
          DISCORD_WEBHOOK_DEFAULT: ${{ secrets.DISCORD_WEBHOOK }}
          DISCORD_WEBHOOK_MEETING: ${{ secrets.DISCORD_WEBHOOK_MEETING_URL }}
        run: |
          set -euo pipefail

          WEBHOOK="$DISCORD_WEBHOOK_DEFAULT"
          LABEL="문서"
          ICON="✅"
          TITLE="문서"
          URL="https://doppleddiggong.github.io/Onepiece/docs/"
          COLOR="5865242"  # Discord 기본 블루
          DESCRIPTION="새로운 문서가 배포되었습니다."

          case "$TRIGGER" in
            "meeting-update")
              WEBHOOK="${DISCORD_WEBHOOK_MEETING:-$DISCORD_WEBHOOK_DEFAULT}"
              LABEL="회의록"
              ICON="📘"
              COLOR="10181046"  # 보라색
              DESCRIPTION="회의록이 업데이트되었습니다."
              ;;
            "daily-update")
              LABEL="Daily DevLog"
              ICON="📅"
              COLOR="5865242"  # Discord 블루
              DESCRIPTION="오늘의 개발 활동이 기록되었습니다."
              ;;
            "weekly-update")
              LABEL="Weekly Report"
              ICON="📊"
              COLOR="16705372"  # Discord 옐로우
              DESCRIPTION="주간 개발 리포트가 생성되었습니다."
              ;;
          esac

          if [ -n "$LATEST_FILE" ]; then
            TITLE=$(basename "$LATEST_FILE" .md)
            URL="https://doppleddiggong.github.io/Onepiece/docs/${LATEST_FILE#Documents/}"
            URL="${URL%.md}.html"
          fi

          if [ -z "$WEBHOOK" ]; then
            echo "⚠️ Discord 웹훅이 설정되지 않았습니다. 알림 생략"
            exit 0
          fi

          # Discord Embed 형식으로 메시지 전송
          PAYLOAD=$(cat <<EOF
          {
            "embeds": [{
              "title": "${ICON} ${LABEL} — ${TITLE}",
              "description": "${DESCRIPTION}",
              "url": "${URL}",
              "color": ${COLOR},
              "footer": {
                "text": "자세한 내용은 링크를 클릭하여 확인하세요."
              },
              "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
            }]
          }
          EOF
          )

          curl -X POST -H "Content-Type: application/json" \
            -d "$PAYLOAD" "$WEBHOOK"

          echo "✅ Discord 알림 전송 완료: ${LABEL} - ${TITLE}"