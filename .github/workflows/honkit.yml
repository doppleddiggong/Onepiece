name: Generate HonKit

on:
  # 다른 워크플로우(Daily/Weekly/Meeting)로부터 빌드 요청을 받을 때만 실행
  repository_dispatch:
    types: [honkit-build]

  # 수동으로 문서를 빌드하고 배포할 때 사용
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Generate SUMMARY.md
        run: |
          python .github/scripts/generate_summary.py
          echo "✅ SUMMARY.md has been generated"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install HonKit and plugins
        run: |
          npm install -g honkit
          npm install -g gitbook-plugin-theme-comscore
          npm install -g gitbook-plugin-github
          npm install -g gitbook-plugin-collapsible-menu
          npm install -g gitbook-plugin-back-to-top-button
          npm install -g gitbook-plugin-search-plus
          npm install -g gitbook-plugin-expandable-chapters-small
          npm install -g gitbook-plugin-page-toc-button
          npm install -g gitbook-plugin-anchor-navigation-ex
          npm install -g gitbook-plugin-code
          npm install -g gitbook-plugin-alerts
          npm install -g gitbook-plugin-emphasize
          npm install -g gitbook-plugin-klipse
          npm install -g honkit-plugin-mermaid

      - name: Build HonKit
        working-directory: Documents
        run: |
          honkit build

      - name: Resolve keep_files flag
        id: keepflag
        env:
          PAYLOAD_KEEP_FILES: ${{ github.event.client_payload.keep_files }}
        run: |
          KEEP_FILES="true"
          if [ -n "${PAYLOAD_KEEP_FILES}" ]; then
            KEEP_FILES="${PAYLOAD_KEEP_FILES}"
          fi
          echo "keep_files=$KEEP_FILES" >> $GITHUB_OUTPUT

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./Documents/_book
          publish_branch: gh-pages
          destination_dir: docs
          enable_jekyll: false
          force_orphan: false
          keep_files: ${{ steps.keepflag.outputs.keep_files == 'true' }}

      - name: Summary
        run: |
          echo "✅ HonKit documentation has been built and deployed!"
          echo "📚 Documentation will be available at: https://doppleddiggong.github.io/Onepiece/docs/"

  notify:
    needs: build-and-deploy
    runs-on: ubuntu-latest
    if: success()
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Send Discord Notification
        env:
          EVENT_NAME: ${{ github.event_name }}
          TRIGGER: ${{ github.event.client_payload.trigger || '' }}
          LATEST_FILE: ${{ github.event.client_payload.latest_file || '' }}
          EVENT_BEFORE: ${{ github.event.before || '' }}
          EVENT_AFTER: ${{ github.sha }}
          DISCORD_WEBHOOK_DEFAULT: ${{ secrets.DISCORD_WEBHOOK }}
          DISCORD_WEBHOOK_MEETING: ${{ secrets.DISCORD_WEBHOOK_MEETING_URL }}
        run: |
          set -euo pipefail

          WEBHOOK="$DISCORD_WEBHOOK_DEFAULT"
          LABEL="문서"
          ICON="✅"
          TITLE=""
          URL="https://doppleddiggong.github.io/Onepiece/docs/"

          case "$TRIGGER" in
            "meeting-update")
              WEBHOOK="${DISCORD_WEBHOOK_MEETING:-$DISCORD_WEBHOOK_DEFAULT}"
              LABEL="회의록"
              ICON="📘"
              ;;
            "daily-update")
              LABEL="일일 일지"
              ICON="🗓️"
              ;;
            "weekly-update")
              LABEL="주간 일지"
              ICON="📅"
              ;;
          esac

          if [ -n "$LATEST_FILE" ]; then
            MD_FILE_PATH="$LATEST_FILE"
          else
            RANGE_BEFORE="$EVENT_BEFORE"
            RANGE_AFTER="$EVENT_AFTER"
            if [ -z "$RANGE_BEFORE" ]; then
              RANGE_BEFORE="HEAD~1"
            fi
            MD_FILE_PATH=$(git diff --name-only "$RANGE_BEFORE" "$RANGE_AFTER" | grep '\.md$' | head -n1 || true)
          fi

          if [ -z "$MD_FILE_PATH" ]; then
            echo "✅ .md 파일 변경사항이 없어 알림을 생략합니다."
            exit 0
          else
            TITLE=$(basename "$MD_FILE_PATH")
            URL="https://doppleddiggong.github.io/Onepiece/docs/${MD_FILE_PATH#Documents/}"
            URL="${URL%.md}.html"
          fi

          MSG="${ICON} ${LABEL} **${TITLE}** 가 배포되었습니다!\n🔗 [바로가기](${URL})"

          if [ -z "$WEBHOOK" ]; then
            echo "⚠️ Discord 웹훅이 설정되지 않았습니다. 알림 생략"
            exit 0
          fi

          curl -X POST -H "Content-Type: application/json" \
            -d "{\"content\": \"$MSG\"}" "$WEBHOOK"
